<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/stylesheets/style.css" />
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/animate.css/4.1.1/animate.min.css" />
    <title>Editing <%= bill.billname %>
    </title>
</head>

<body>
    <%- include('../partials/_navbar.ejs') %>
        <h1>Edit <%= bill.billname %>
        </h1>
        <form action="/users/<%= user._id %>/bills/<%= bill._id %>?_method=PUT" method="POST">

            <label for="billname">Bill:</label>
            <input type="text" name="billname" id="billname" value="<%= bill.billname %>" />

            <label for="amount">Amount:</label>
            <input type="text" name="amount" id="amount" value="<%= bill.amount %>" />

            <label for="date">Select Due-Date:</label>
            <input type="date" id="date" name="date" value="<%= bill.date.toISOString().slice(0, 10) %>">


            <label for="isPaid">Paid?</label>
            <input type="checkbox" name="isPaid" <% if (bill.isPaid) { %>checked<% } %> >

                <button type="submit">Update</button>
        </form>
</body>

</html>